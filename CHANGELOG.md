# 📋 班級課表對照機 - 更新歷程 (CHANGELOG)

---

## 🗓️ 2026-04-20（下午版本 — 穩定重建工程）

> 本次更新為全面性的除錯、重建與 UI 優化。多個核心錯誤被修正，介面風格確立為 Google Calendar 白底清爽風格。

### 🐛 修復的 Bug（按發生順序）

| # | 問題描述 | 修復方式 | Commit |
|---|---|---|---|
| 1 | CSS 路徑錯誤：HTML 寫 `index.css`，實際檔案為 `app.css`，導致介面全白無樣式 | 修正 `<link>` 路徑 | `08c7833` |
| 2 | `const DAYS` 被誤刪，導致 `ReferenceError: DAYS is not defined`，程式完全停止 | 補回常數定義 | `247a80e` |
| 3 | `index.html` 在重構過程中被截斷（僅剩 133 行），下半部所有按鈕與結構消失，點不了 | 完整重寫 HTML | `1afb3c2` |
| 4 | `clearAllSchedules()` 函數被誤刪，HTML 清除按鈕按了沒反應 | 補回完整函數 | `247a80e` |
| 5 | `init()` 被雙重呼叫（DOMContentLoaded 與檔案末尾各一次），造成資料競爭 | 移除重複呼叫 | `247a80e` |
| 6 | `id="sidebarToggle"` 在 HTML 重構時遺失，`setupMobileSidebar()` 噴出 `TypeError: Cannot set properties of null` | 補回隱藏按鈕 | `6cd5126` |
| 7 | 賽程規劃頁格子只顯示科目名稱，任課老師不見了 | 修復 `renderSchedulingTable` 中 `raw.split('|')` 未取 teacher 欄位 | `e87f870` |
| 8 | 待排清單出現重複場次（視覺上同一場出現兩次） | 追加起動時幽靈排程自動清理（matchId 不存在者自動移除） | `e1ba8c6` |
| 9 | 有一筆排程指向不存在的 matchId（顯示為「未知」），干擾渲染 | `init()` 啟動時自動偵測並刪除幽靈排程，同步更新 Firestore | `e1ba8c6` |

---

### ✨ 新增功能

- **[賽程規劃]** 在「賽程規劃」Tab 頂部加入週次切換按鈕（`‹` / `›` / 今天），不需再切回「課表對照」Tab 才能換週。兩個 Tab 的週次顯示同步更新。
- **[除錯工具]** 新增「🔍 診斷」按鈕，可一鍵查看：
  - 賽事總數
  - 重複 ID 數量（是否有問題資料）
  - 已排定場數
  - 最近 5 筆排定賽程詳情
- **[資料保護]** 載入時自動去重 `matches.json`（依 ID 過濾），避免 JSON 重複條目進入記憶體。

---

### 🎨 UI 設計重建

| 階段 | 風格 | 結果 |
|---|---|---|
| 第一版 | 深色科幻風（深石板藍 + 發光）| ❌ 使用者：「給外星人看的嗎？」|
| 第二版 | 標準商務白底藍字 | ❌ 使用者：「尺寸不對」|
| **第三版（現行）** | **Google Calendar 極簡風** | ✅ 確認採用 |

**現行設計規格：**
- 字型：Roboto + Noto Sans TC
- 背景：`#f8f9fa`（Google 標準灰白）
- 邊框：`#dadce0`（Google 標準細線）
- 主色：`#1a73e8`（Google Blue）
- 格子狀態：柔和淡彩（橘/綠/黃）不刺眼
- 週次按鈕：36×36px 圓形，今天按鈕改為藍底白字膠囊形

---

### 📊 系統現況（截至 2026-04-20 22:13）

- **賽事總數**：106 場（0 重複）
- **已排定**：57 場
- **幽靈排程**：1 筆（已自動清除）

---

## 🗓️ 2026-04-20（上午版本）

- **[視覺]** 根據使用者需求，介面視覺主題全面由深色改為**白底黑字 (Light Mode)**，大幅提升閱讀對比度與列印清晰度。
- **[資料]** 全面更新國中部與高中部最新賽事名單：
  - 新增國中羽球「國二4 張釉棠 VS 國三3」與「國一1 VS 國一5 蔡嘉澤」正式對戰。
  - 新增高中羽球「高二孝 VS 高一信 黃信錩」正式對戰，並同步修正後續種子位。
- **[清理]** 自動過濾並移除所有標記為「✅ 已結束」的場次，使「待排賽事」清單保持精簡高效。

---

## 🗓️ 2026-04-19

- **[功能]** 新增賽程 JSON 資料的「下載檔 / 匯入檔」功能，方便將本地儲存狀態遷移至雲端或備份。
- **[修復]** 處理國中部班級（如國一、國二、國三）比對邏輯，新增無視空格比對器確保「國二4」能成功讀取課表。
- **[機制]** 完成「本週已排定賽程」區塊的「一鍵展開 / 收合」按鈕，解決大區塊遮擋主課表視線的問題。
- **[新增]** 強化的賽程防呆與衝突警告機制，當同一人同一天有兩場賽程時會立刻標示高對比的警戒紅字 ⚠️。
- **[視覺]** 自行繪製 SVG 高對比專屬「羽毛球」圖示，並修正已排定清單以鮮豔綠松色背景強化提示。
- **[重大功能]** 全新加入獨立的 **「賽程排定 (Scheduling)」** 操作頁籤：
  - 支援點擊空堂即時帶入比賽的自動系統。
  - 即刻顯示籃球 (限制2場)、排球 (限制2場)、羽球 (限制1場) 球場容量與衝突。
- `ba5e0f9` **[修復]** 新增「精進學習」關鍵字至彈性時段清單，使精進學習時間可被勾選為推薦排課時段。

---
